/** Логотип FREYA — Jost Medium, широкий трекинг (как на бренд-мокапе). */
export function SalonLogo({ className = "" }: { className?: string }) {
  return (
    <span className={`logo-freya text-ink ${className}`} aria-label="FREYA">
      FREYA
    </span>
  );
}
